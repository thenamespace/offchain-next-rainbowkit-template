# Required for react-avatar-editor compatibility with React 19
# react-avatar-editor@13.0.2 only supports React 16-18
# Use legacy peer deps until a React 19 compatible version is available
legacy-peer-deps=true
